<%
  require 'base64'
  require 'digest/sha1'
  password = 'password' # scope.lookupvar('password')
  password_hash = '{SHA-1}' << Base64.encode64( Digest::SHA1.digest( password ) )
%>

<%= password_hash %> 
